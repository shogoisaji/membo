import 'package:flutter/material.dart';

class BoardSettingPage extends StatelessWidget {
  const BoardSettingPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Board Setting Page'),
      ),
      body: const Center(
        child: Text('Board Setting Page'),
      ),
    );
  }
}
